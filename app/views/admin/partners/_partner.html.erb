<tr>
  <td><%= partner.name %></td>
  <td>
    <%= link_to icon('edit', 'modifier'), edit_admin_partner_path(partner), class: 'btn btn-xs btn-default' %>
    <%= link_to icon('times', 'supprimer'), admin_partner_path(partner), class: 'btn btn-xs btn-danger', method: :delete, data: { confirm: 'Etes vous sur ?' } %>
  </td>
</tr>
